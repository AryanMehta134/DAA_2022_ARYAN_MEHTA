#include <bits/stdc++.h>
using namespace std;

int main(){
    vector<int> v;
    bool f = false;
    int num,n;
    cin>>num;
    cin>>n;
    while(n--){
        int x;
        cin>>x;
        v.push_back(x);
    }
    for(int i=0; i<v.size(); i++{
        if(v[i] == num){
            cout<<"Present "<<i+1<<endl;
            f=true;
            break;
        }
    }
    if(!f){
        cout<<"Not present "<<v.size()<<endl;
    }
    return 0;
}
